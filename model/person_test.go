package model

/* import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestCreateDeletePerson(t *testing.T) {
	//persons, _ := GetPersons()
	//assert.Equal(t, persons, []Person{})

	want := Person{Username: "a", Birthday: "1987-08-21"}
	CreatePerson(want)
	got, _ := FindPerson("a")
	assert.Equal(t, want.Username, got.Username)
	assert.Equal(t, want.Birthday, got.Birthday)
	SoftDeletePerson(want)
}

func TestGetPersons(t *testing.T) {
	FindPersons()
} */
